# Runner labels newer than actionlint's bundled list.
#
# macos-15-intel is a current GitHub-hosted runner, and it is the only way to
# build and test the macOS x86-64 package. These labels were checked against
# GitHub's runner reference; future migrations happen in reviewed changes here.
self-hosted-runner:
  labels:
    - macos-15-intel
